@model SubEntityFormViewModel


<form asp-action="@(Model?.Id > 0 ? "Edit" : "Create")"
      data-ajax-method="post"
      data-ajax="true"
      data-ajax-begin="onModalBegin"
      data-ajax-success="onModalSuccess"
      data-ajax-failure="showErrorMessage"
      data-ajax-complete="onModalComplete">
    @if (Model?.Id > 0)
    {
        <input type="hidden" asp-for="Id" />
        <input type="hidden" asp-for="IsInside" />
    }
    
   @*  <div class="mb-5">
        <div class="form-floating mb-2">
            <select style="height: 60px;" class="form-select form-select-solid js-select2" data-placeholder="اختار جهه" asp-for="EntityId" asp-items="@Model.Entities">
                <option value="">اختار جهه</option>
            </select>
            <label asp-for="EntityId"></label>
        </div>
        <span asp-validation-for="EntityId" class="text-danger d-block"></span>
    </div> *@

    <div class="d-flex flex-column mb-8 fv-row fv-plugins-icon-container">
        <!--begin::Label-->
        <label class="d-flex align-items-center fs-6 fw-semibold mb-2">
            <span class="required">الجهة</span>
        </label>
        <!--end::Label-->
        <!--begin::Row-->
        <div class="row">
            <!--begin::Col-->
            <div class="fv-row fv-plugins-icon-container fv-plugins-bootstrap5-row-invalid">
                <select style="height: 60px;" class="form-select form-select-solid js-select2" data-placeholder="اختار جهه" asp-for="EntityId" asp-items="@Model.Entities">
                    <option value="">اختار جهه</option>
                </select>
                <span asp-validation-for="EntityId" class="text-danger d-block"></span>
            </div>
            <!--end::Col-->
        </div>
        <!--end::Row-->
    </div>

     <div class="d-flex flex-column mb-8 fv-row fv-plugins-icon-container">
        <!--begin::Label-->
        <label class="d-flex align-items-center fs-6 fw-semibold mb-2">
            <span class="required">اسم المستوى الاول للجهه</span>
        </label>
        <!--end::Label-->
        <!--begin::Row-->
        <div class="row">
            <!--begin::Col-->
            <div class="fv-row fv-plugins-icon-container fv-plugins-bootstrap5-row-invalid">
                <input type="text" class="form-control" placeholder="اسم المستوى الاول للجهه" asp-for="Name" />
                <span asp-validation-for="Name" class="text-danger d-block"></span>
            </div>
            <!--end::Col-->
        </div>
        <!--end::Row-->
    </div> 

   @*  <div class="mb-5">
        <div class="form-floating mb-2">
            <input type="text" class="form-control" placeholder="Name" asp-for="Name" />
            <label style="margin-right:30px" asp-for="Name"></label>
        </div>
        <span asp-validation-for="Name" class="text-danger d-block"></span>
    </div>
 *@
    <partial name="_ModalFooter" />
</form>
@section Scripts
{
    <partial name="_ValidationScriptsPartial" />
    <script src="~/js/book-form.js" asp-append-version="true"></script>
}